# ==============================================================================
# ENGLISH MATE — AI ENGLISH BRIDGE ENVIRONMENT CONFIGURATION
# ==============================================================================

NODE_ENV=development
PORT=5000

# ------------------------------------------------------------------------------
# DATABASE CONFIGURATION
# ------------------------------------------------------------------------------
# DB_CLIENT: 'mysql' (default for production) or 'sqlite' (zero-friction fallback)
DB_CLIENT=mysql
DB_HOST=localhost
DB_PORT=3306
DB_NAME=englishmate
DB_USER=root
DB_PASSWORD=

# SQLite fallback path (used automatically when MySQL is unavailable in dev)
SQLITE_PATH=./database/englishmate.sqlite

# ------------------------------------------------------------------------------
# JWT AUTHENTICATION
# ------------------------------------------------------------------------------
JWT_SECRET=englishmate_super_secret_production_jwt_key_2026_change_in_prod
JWT_EXPIRES_IN=7d

# ------------------------------------------------------------------------------
# AI TUTOR & PROVIDER CONFIGURATION
# ------------------------------------------------------------------------------
# AI_PROVIDER: 'gemini', 'openai', 'anthropic', or 'rule_engine'
AI_PROVIDER=gemini
AI_API_KEY=
AI_MODEL=gemini-1.5-flash

# ------------------------------------------------------------------------------
# CORS & FRONTEND
# ------------------------------------------------------------------------------
CORS_ORIGIN=*
